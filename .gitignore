# Python bytecode
__pycache__/
*.py[cod]
*$py.class
*.pyo

# Virtual environments
.venv/
venv/
env/

# Distribution / packaging
*.egg-info/
dist/
build/

# Local config (per-clone, gitignored per CLAUDE.md)
config.json

# Vault (per-project, gitignored per CLAUDE.md)
vault/

# ChromaDB (per-project)
chroma_db/

# PDF files (user-supplied, never committed — except test fixtures)
*.pdf
!tests/fixtures/*.pdf
tests/fixtures/test_manuel*.pdf

# Registry (shared across clones, not in repo)
papers_registry.json

# IDE / OS artifacts
.DS_Store
Thumbs.db
.idea/
.vscode/
.obsidian/

# Claude Code local settings and memory
.claude/

# pytest cache
.pytest_cache/

# Coverage reports
.coverage
htmlcov/

#user files
NOTES.md
STARTING_POINT.md

# Local scratch — registry copies, capture scripts, ad-hoc test text (never committed)
.local/

# MinerU output artifacts (persistent, git-ignored per D-16)
.mineru_output/